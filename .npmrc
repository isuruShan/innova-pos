# Required for Vite 8 / Rolldown native bindings (@rolldown/binding-linux-x64-gnu, etc.)
optional=true

# Install optional platform bindings for Linux when lockfile was created elsewhere
supported-architectures-os=linux,darwin,win32
supported-architectures-cpu=x64,arm64
